FROM nginx:alpine

# Copy nginx config
COPY nginx.ecs.conf /etc/nginx/conf.d/default.conf

# Copy frontend static files into nginx html folder
# IMPORTANT: this assumes your frontend is inside gateway/frontend/
COPY frontend/ /usr/share/nginx/html

EXPOSE 80
